<Window x:Class="SequenceClassificationModel.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SequenceClassificationModel.WPF.ViewModels"
        Title="Arseni Kuskou" Height="700" Width="1100"
        Background="#0A0A0A" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <SolidColorBrush x:Key="AccentColor" Color="#00A3FF"/>
        <SolidColorBrush x:Key="CardBg" Color="#111111"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#222222"/>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Grid.Column="2" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="#1A1A1A" BorderBrush="#333333" BorderThickness="1" CornerRadius="2">
                                            <Path x:Name="Arrow" Fill="White" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0" Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Arrow" Property="Fill" Value="#00A3FF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False" Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,3,23,3" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <TextElement.Foreground>White</TextElement.Foreground>
                            </ContentPresenter>

                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid x:Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder" Background="#1A1A1A" BorderThickness="1" BorderBrush="#333333"/>
                                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00A3FF"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FlatButton" TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#00A3FF"/>
                    <Setter Property="Background" Value="#222222"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="35"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#050505" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel Margin="30,0">
                <TextBlock Text="Arseni Kuskou" DockPanel.Dock="Right" VerticalAlignment="Center" 
                           Foreground="#444" FontSize="12" FontWeight="Light"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Rectangle Width="3" Height="18" Fill="{StaticResource AccentColor}" Margin="0,0,15,0"/>
                    <TextBlock Text="SEQUENCE CLASSIFIER" FontSize="14" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Margin="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="40"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="КОНФИГУРАЦИЯ" FontSize="11" Foreground="{StaticResource AccentColor}" FontWeight="Bold" Margin="0,0,0,15"/>

                <Border Background="{StaticResource CardBg}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Алгоритм" Foreground="#666" FontSize="10" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding AvailableAlgorithms}" SelectedItem="{Binding SelectedAlgorithm}" Margin="0,0,0,20"/>

                        <TextBlock Text="Путь к данным" Foreground="#666" FontSize="10" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox Text="{Binding SelectedFolderPath}" IsReadOnly="True" Background="#0A0A0A" Foreground="#555" 
                                     BorderBrush="#222" Padding="8" FontSize="10" VerticalAlignment="Center"/>
                            <Button Grid.Column="1" Content="..." Style="{StaticResource FlatButton}" Margin="5,0,0,0" Padding="10,2" Command="{Binding SelectFolderCommand}"/>
                        </Grid>

                        <TextBlock Text="Размер тестовой выборки" Foreground="#666" FontSize="10" Margin="0,0,0,5"/>
                        <DockPanel Margin="0,0,0,15">
                            <TextBlock Text="{Binding TestSizePercentage, StringFormat={}{0}%}" DockPanel.Dock="Right" Width="35" TextAlignment="Right" Foreground="White" FontSize="11"/>
                            <Slider Minimum="10" Maximum="50" Value="{Binding TestSizePercentage}" IsSnapToTickEnabled="True" TickFrequency="5"/>
                        </DockPanel>

                        <CheckBox Content="Провести тестирование после обучения" IsChecked="{Binding RunTesting}" 
                                  Foreground="#AAA" FontSize="11" Margin="0,0,0,25" Cursor="Hand"/>

                        <Button Content="ОБУЧИТЬ СИСТЕМУ" Style="{StaticResource FlatButton}" 
                                Command="{Binding TrainTestCommand}" Height="40" Background="#00A3FF" Foreground="Black" FontWeight="Bold"/>

                        <Grid Margin="0,15,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button Grid.Column="0" Content="СОХРАНИТЬ" Style="{StaticResource FlatButton}" Command="{Binding SaveModelCommand}" FontSize="10"/>
                            <Button Grid.Column="2" Content="ЗАГРУЗИТЬ" Style="{StaticResource FlatButton}" Command="{Binding LoadModelCommand}" FontSize="10"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding AnalyzeHeader}" FontSize="11" Foreground="{StaticResource AccentColor}" FontWeight="Bold" Margin="0,0,0,15"/>

                <Border Grid.Row="1" Background="{StaticResource CardBg}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="30" IsEnabled="{Binding IsModelTrained}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Button Content="ВЫБРАТЬ ПАПКУ" Style="{StaticResource FlatButton}" Command="{Binding SelectSingleImageCommand}" HorizontalAlignment="Left" Margin="0,0,0,20"/>

                        <Border Grid.Row="1" Background="#050505" BorderBrush="#222" BorderThickness="1" MaxWidth="350" MaxHeight="250" VerticalAlignment="Top" HorizontalAlignment="Center">
                            <Grid>
                                <TextBlock Text="Превью первого кадра" Foreground="#333" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="10"/>
                                <Image Source="{Binding PreviewImagePath}" Stretch="Uniform" Margin="5"/>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Row="2" Margin="0,20,0,0">
                            <Button Content="ЗАПУСТИТЬ РАСПОЗНАВАНИЕ" Style="{StaticResource FlatButton}" Height="45" FontWeight="Bold"
                                    Command="{Binding PredictSingleCommand}" Background="#1A1A1A" Foreground="White" Margin="0,0,0,15"/>

                            <Border Background="#0A0A0A" Padding="15" BorderBrush="#222" BorderThickness="1">
                                <TextBlock Text="{Binding SinglePredictionResult}" 
                                           Foreground="{StaticResource AccentColor}" FontSize="14"
                                           TextWrapping="Wrap" TextAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="#050505" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="30,0">
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" FontSize="10" Foreground="#333"/>
                <ProgressBar Height="1" IsIndeterminate="True" Foreground="{StaticResource AccentColor}" Background="Transparent" BorderThickness="0" VerticalAlignment="Bottom"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"/>
            </Grid>
        </Border>
    </Grid>
</Window>